default:
  suites:
    core:
      contexts:
        - BasketContext
    ui:
      contexts:
        - WebBasketContext
      filters:
        tags: '@critical'
  extensions:
    Behat\MinkExtension:
      base_url: http://localhost:8000
      sessions:
        default:
          goutte: ~
